# OTP Security Demo

This project demonstrates an OTP request and verification flow with a small Node.js API and browser frontend.

## Development

```text
npm install
npm start
```

The app runs at `http://localhost:3000`.